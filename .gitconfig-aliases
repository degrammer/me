alias.wip !read -p "Enter branch name: " name && git checkout -b "wip/$name"
alias.ready !git branch -m "$(git branch --show-current | sed "s/wip\///g")"
alias.copy !git branch --show-current | pbcopy
alias.alias !git config --get-regexp alias